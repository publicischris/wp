@extends('layouts.app')@section('content')<h1>Wochenblick</h1><div class="grid">@foreach(['Geplante Kampagnen'=>$planned,'Veröffentlicht letzte Woche'=>$published,'Aktive Themen'=>$topics,'Performance'=>$performances,'Lagechecks'=>$checks] as $title=>$list)<div class="card"><h2>{{ $title }}</h2>@forelse($list as $i)<p><strong>{{ $i->title ?? $i->summary ?? $i->campaign?->title }}</strong><br><span class="muted">{{ $i->status ?? $i->rating ?? '' }}</span></p>@empty<p class="muted">Keine Einträge.</p>@endforelse</div>@endforeach</div>@endsection
