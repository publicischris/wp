@extends('layouts.app')@section('content')<h1>{{ $label }}</h1><p><a class="btn" href="{{ route($v.'.create') }}">Neu anlegen</a></p><div class="card"><table><tr><th>Titel/Name</th><th>Status</th><th></th></tr>@foreach($items as $item)<tr><td>{{ $item->title ?? $item->name }}</td><td><span class="badge">{{ $item->status ?? $item->industry ?? '' }}</span></td><td><a href="{{ route($v.'.edit',$item) }}">Bearbeiten</a> · <form method="post" action="{{ route($v.'.destroy',$item) }}" style="display:inline">@csrf @method('delete')<button>löschen</button></form></td></tr>@endforeach</table></div>@if(str_starts_with($v,'settings.'))<div class="card"><a href="{{ route('settings.channels.index') }}">Kanäle</a> · <a href="{{ route('settings.competitors.index') }}">Wettbewerber</a> · <a href="{{ route('settings.benchmarks.index') }}">Benchmark-Unternehmen</a> · <a href="{{ route('settings.tenants.index') }}">Mandanten</a></div>@endif @endsection
