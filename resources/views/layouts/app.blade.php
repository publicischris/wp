<!doctype html><html lang="de"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="csrf-token" content="{{ csrf_token() }}"><title>Content Compass</title><style>body{font-family:system-ui,Arial;margin:0;background:#f8fafc;color:#1e293b}.wrap{max-width:1120px;margin:auto;padding:24px}.top{background:#fff;border-bottom:1px solid #e2e8f0}.nav{display:flex;gap:14px;align-items:center}.nav a{color:#334155;text-decoration:none}.card{background:white;border:1px solid #e2e8f0;border-radius:12px;padding:18px;margin:12px 0}.grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(240px,1fr));gap:14px}input,textarea,select{width:100%;padding:10px;border:1px solid #cbd5e1;border-radius:8px}label{font-weight:600;font-size:14px}button,.btn{background:#0f766e;color:white;border:0;border-radius:8px;padding:9px 12px;text-decoration:none;display:inline-block}.muted{color:#64748b}.badge{background:#e2e8f0;border-radius:999px;padding:3px 8px;font-size:12px}table{width:100%;border-collapse:collapse}td,th{padding:10px;border-bottom:1px solid #e2e8f0;text-align:left}</style></head><body><div class="top"><div class="wrap nav"><strong>Content Compass</strong>@auth <a href="{{ route('dashboard') }}">Wochenblick</a><a href="{{ route('calendar.index') }}">Kalender</a><a href="{{ route('topics.index') }}">Themen</a><a href="{{ route('assets.index') }}">Inhalte</a><a href="{{ route('campaigns.index') }}">Kampagnen</a><a href="{{ route('signal-checks.index') }}">Lagecheck</a><a href="{{ route('settings.tenants.index') }}">Einstellungen</a><form method="post" action="{{ route('tenant.switch') }}" style="margin-left:auto">@csrf<select name="tenant_id" onchange="this.form.submit()">@foreach($navTenants??[] as $t)<option value="{{ $t->id }}" @selected(($activeTenant?->id??null)==$t->id)>{{ $t->name }}</option>@endforeach</select></form><form method="post" action="{{ route('logout') }}">@csrf<button>Logout</button></form>@endauth</div></div><main class="wrap">@if(session('status'))<div class="card">{{ session('status') }}</div>@endif @yield('content')</main></body></html>
