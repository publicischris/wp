<?php namespace App\Services; use App\Models\SignalCheck; class AiBriefingService{ public function generateSignalCheckSummary(SignalCheck $signalCheck): array { $signalCheck->loadMissing('tenant','topic'); return ['ai_summary'=>trim(($signalCheck->topic?->title ?? 'Thema').' für '.($signalCheck->tenant?->name ?? 'Mandant').': '.($signalCheck->market_signal_notes ?: 'Keine Marktnotizen erfasst.')),'cautious_next_steps'=>'Manuelle Beobachtungen prüfen, vorhandene Inhalte sichten und nächste kleine Kommunikationsmaßnahme planen.']; }}
